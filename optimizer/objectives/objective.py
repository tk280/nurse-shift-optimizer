from __future__ import annotations

from typing import Any, Iterable


def build_objective(model: Any, penalty_terms: Iterable[Any]) -> None:
    """
    Aggregate penalty_terms and set the objective on the model.
    Placeholder for refactor. No-op for now.
    """
    return
